name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      MEM0_TELEMETRY: "false"
      MEM0_CHROMA_PATH: /tmp/autolinkingbrain-chroma-ci
      MCP_HYBRID_SEARCH: "1"
      MEM0_INSTALL_SYNC_DISCOVERED: "0"
      CODEGRAPH_AUTO_DISCOVER: "0"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-lock.txt

      - name: Run tests
        run: python -m pytest tests/ -q --tb=short
