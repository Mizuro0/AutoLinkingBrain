# Optional manual extras only — auto-discovery is ON by default (same rules as Mem0 slug).
# brain.py codegraph init finds git repos automatically:
#   - sibling folders next to this repo (e.g. ../backend when cloned under ~/projects)
#   - child repos under CODEGRAPH_WORKSPACE or cwd (monorepo: workspace/backend, …)
#
# Uncomment only if a repo is NOT found automatically:
# ../orphan_repo
